To 1. convert tables/*.xls(x) to .csv
   2. get nodes/edges from csv/*.csv

1. copy convert_all_files.sh, parse_passing.py, and excel_to_csv.py
to your working directory (with csv, tables dir's)
2. run the following:

> bash convert_all_files.sh


